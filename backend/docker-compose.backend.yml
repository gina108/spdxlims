version: "3.9"

services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: lims_db
      POSTGRES_USER: lims_user
      POSTGRES_PASSWORD: lims_password
    ports:
      - "5432:5432"
    volumes:
      - lims_pgdata:/var/lib/postgresql/data

volumes:
  lims_pgdata:
